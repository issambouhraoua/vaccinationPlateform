{% extends 'base.html.twig' %}

{% block title %}Vaccin index{% endblock %}

{% block body %}
<div class="container-fluid">
    <div class="d-sm-flex align-items-center justify-content-between mb-4">
        <h1 class="h3 mb-0 text-gray-800">Liste des vaccins</h1>
        
    </div>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom_vaccin</th>
                <th>Description</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for vaccin in vaccins %}
            <tr>
                <td>{{ vaccin.id }}</td>
                <td>{{ vaccin.nomVaccin }}</td>
                <td>{{ vaccin.description }}</td>
                <td>
                    <a type="button" class="btn btn-primary" href="{{ path('vaccin_show', {'id': vaccin.id}) }}">Voir</a>
                    <a type="button" class="btn btn-secondary" href="{{ path('vaccin_edit', {'id': vaccin.id}) }}">Mettre à jour</a>
                    
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('vaccin_new') }}"  type="button" class="btn btn-success">Ajouter un nouveau vaccin</a>
{% endblock %}
